const setup = () => {
}
window.addEventListener("load", setup);
window.alert('ok');
const confirm = window.confirm("weet u het zekrer")
const prompt= window.prompt("jeeps")
console.log(confirm);
console.log(prompt);
